const LOGIN_SUCESS = 1
const LOGIN_FAIL = 0

// Vai retornar o resultado do login e o token de autenticação
const loginSubmiter = async (userData) => {
    // Inserir código de requisição HTTP

}

// Função que verifica se o usuário está logado
// Realizando uma requisição para o backend
const isLogged = () => {

}